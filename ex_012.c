#include <stdio.h>

/*
 * Problem 12:
 * What is the value of the first triangle number to have over five hundred divisors?
 * 
 * Result: 
 */
int main() {
    int continuer = 1;
    while (continuer) {
        
    }
    printf("%ld\n", sum);
}